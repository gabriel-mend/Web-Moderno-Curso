const [a, b, c, d] = [3, 5, 1, 15]
// operador binário
const soma = a + b + c + d // infix operador1 + operador2
const subtracao = d - b
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2

console.log(soma, subtracao, divisao, modulo)